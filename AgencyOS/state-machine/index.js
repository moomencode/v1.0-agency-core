import { StateMachine, createStateMachine, STATES, START_STATE, END_STATES } from './engine.js';

export { StateMachine, createStateMachine, STATES, START_STATE, END_STATES };
export const STATE_MACHINE_API_VERSION = '1.0';
