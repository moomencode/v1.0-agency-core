{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://agency.os/workflows/website-generation",
  "title": "WebsiteGenerationWorkflow",
  "type": "object",
  "description": "Config bundle construction workflow definition.",
  "required": ["id", "name", "trigger", "actors", "steps", "exitConditions"],
  "properties": {
    "id": { "const": "website-generation" },
    "name": { "const": "Website Generation" },
    "trigger": {
      "type": "string",
      "description": "BusinessDossier with sufficient confidence"
    },
    "actors": {
      "type": "array",
      "const": ["content-writer", "website-builder"]
    },
    "steps": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "actor", "action", "output", "gate"],
        "properties": {
          "id": { "type": "string" },
          "actor": { "type": "string" },
          "action": { "type": "string" },
          "output": { "type": "string" },
          "gate": {
            "type": "object",
            "properties": {
              "type": { "type": "string" },
              "condition": { "type": "string" }
            }
          }
        }
      },
      "minItems": 1
    },
    "exitConditions": {
      "type": "object",
      "properties": {
        "success": { "type": "string" },
        "fail": { "type": "string" }
      }
    },
    "entryDocument": { "type": "string" },
    "exitDocument": { "type": "string" }
  },
  "additionalProperties": false
}
